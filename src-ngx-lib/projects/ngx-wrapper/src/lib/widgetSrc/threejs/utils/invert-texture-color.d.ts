import * as THREE from 'three';
/**
 * Returns texture with inverted colors
 */
export declare function invertTextureColor(texture: THREE.Texture, isAlphaInverted?: boolean): THREE.CanvasTexture;
